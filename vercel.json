{
  "version": 2,
  "name": "fractionalized-ordinals-defi",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_NETWORK_ENV": "production"
  },
  "build": {
    "env": {
      "NEXT_PUBLIC_NETWORK_ENV": "production"
    }
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}